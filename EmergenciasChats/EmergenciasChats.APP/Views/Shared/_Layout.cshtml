@*<!DO TYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    <link href="~/Content/Site.css" rel="stylesheet" type="text/css" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" type="text/css" />
    <script src="~/Scripts/modernizr-2.6.2.js"></script>
</head>
<body>
    <div class="navbar fixed-top navbar-dark bg-info text-dark" style="background-color: #e3f2fd; position: sticky;">
    
        <div class="container">



            @if (Request.IsAuthenticated)
            {
                <a href="/Admin/Detalle?id={ {@item.Imagen} }">Update</a>
                @Html.ActionLink("Login", "Login", "Admin", new { area = "" }, new { @class = "navbar-brand text-light" })
                @Html.ActionLink("Cerrar Sesión", "Logout", "Admin", new { area = "" }, new { @id = "home", @class = "navbar-brand text-secondary" })
            }
            else
            {
                @Html.ActionLink("Inicio", "Index", "Home", new { area = "" }, new { @id = "home", @class = "navbar-brand text-secondary" })

                <a style="color:snow" href="@Url.Action("Inicio", "Index", "Home")" class="nav-link text-right">Home </a>  <i class="fas fa-home "></i>
                @Html.ActionLink("Cerrar Sesión", "Logout", "Admin", new { area = "" }, new { @id = "home", @class = "navbar-brand text-secondary" })

            }
        </div>
     


    </div>

    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - My ASP.NET Application</p>
        </footer>
    </div>

    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
</body>
</html>*@



<!---->
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Emergencias Chats</title>
    @*<link href="~/Content/Site.css" rel="stylesheet" type="text/css" />*@
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <script src="~/Scripts/modernizr-2.6.2.js"></script>
</head>
<body>

    <nav class="navbar fixed-top navbar-dark bg-info text-dark" style="background-color: #e3f2fd; position: sticky;">
        <div class="container">


            @if (Request.IsAuthenticated)
            {
                @Html.ActionLink("Perfil", "Perfil", "Admin", new { area = "" }, new { @class = "navbar-brand text-light" })
                @*@Html.ActionLink("Cerrar Sesión", "Logout", "Admin", new { area = "" }, new { @class = "navbar-brand text-light" })*@
                @Html.ActionLink("Cerrar Sesión", "Logout", "Admin", new { area = "" }, new { @id = "home", @class = "navbar-brand text-secondary" })
            }
            else
            {
                @*@Html.ActionLink("Inicio", "Index", "Cliente", new { area = "" }, new { @id = "home", @class = "navbar-brand text-secondary" })*@
                       @Html.ActionLink("Login", "Login", "Admin", new { area = "" }, new { @class = "navbar-brand text-light" })
            }
        </div>
    </nav>

    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Chats</p>
        </footer>
    </div>


    <script src="~/scripts/jquery-3.0.0.min.js"></script>
    <script src="~/scripts/bootstrap.min.js"></script>
</body>
</html>